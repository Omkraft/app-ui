name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    uses: Omkraft/.github/.github/workflows/release.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

